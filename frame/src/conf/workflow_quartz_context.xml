<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:jee="http://www.springframework.org/schema/jee"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="
			http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-2.5.xsd
			http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
			http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-2.5.xsd
			http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-2.5.xsd
			http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-2.5.xsd">


	<!-- Quartz Scheduler, with pre-registered triggers -->
	<!-- Will automatically start scheduling on context startup -->
	<bean id="scheduler" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="triggers">
			<list>
				<ref local="sendTaskTrigger"/>
			</list>
		</property>
	</bean>

	<!-- Job definition for sendTaskQuartzJob -->
	<bean id="sendTaskJobDetail" class="org.springframework.scheduling.quartz.JobDetailBean">
                <property name="beanName">
                  <value>sendTaskJobDetailProxy</value>
                </property>
                <property name="jobClass" value="com.izhi.workflow.job.SendTaskQuartzJob">
		</property>
		<property name="jobDataAsMap">
			<map>
                          <entry key="sessionFactory"><ref bean="sessionFactory"/></entry>
                          <entry key="flowTaskManager"><ref bean="flowTaskService"/></entry>
                          <entry key="mailSender"><ref local="mailSender"/></entry>
						  <entry key="userService"><ref bean="userService"/></entry>
                          <entry key="mailFrom"><value>${mail.from}</value></entry>
                          <entry key="mailText"><value>${mail.text}</value></entry>
                          <entry key="mailTitle"><value>${mail.title}</value></entry>
			</map>
		</property>
	</bean>

	<!-- Trigger for the job defined above -->
	<!-- Registered by the 'scheduler' bean -->
	<bean id="sendTaskTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerBean">
		<property name="jobDetail"><ref bean="sendTaskJobDetail"/></property>
		<property name="startDelay"><value>43200000</value></property>
		<property name="repeatInterval"><value>43200000</value></property>
	</bean>

        <!-- MailSender implementation for JavaMail -->
	<!-- Used by sendTaskQuartzJob -->
	<bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
		<property name="host"><value>${mail.host}</value></property>
	</bean>
</beans>
